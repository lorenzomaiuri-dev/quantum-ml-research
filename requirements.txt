# Consolidated dependencies for all four experiments.
# Install the shared framework with:  pip install -e .
# Then install any experiment-specific extras below as needed.
#
# Core — required by all experiments
torch>=2.0
torchvision>=0.15
pennylane>=0.30
numpy>=1.24

# MedMNIST experiments (02, 03, 04)
medmnist>=3.0
scikit-learn>=1.3

# Training infrastructure (02, 03)
tensorboard>=2.14
tqdm>=4.65

# Experiment 01 extras (text generation)
# torchinfo and torchviz are optional; training works without them
# torchinfo>=1.8
# torchviz>=0.0.2

# GPU acceleration (optional — install separately if needed)
# pennylane-lightning-gpu  — requires CUDA toolkit
