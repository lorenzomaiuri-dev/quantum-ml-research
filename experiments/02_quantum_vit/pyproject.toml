[project]
name = "quantum-vit"
version = "0.1.0"
description = "Quantum ViT — VQC patch embedding for medical image classification"
readme = "README.md"
requires-python = ">=3.12"
# All dependencies are managed from the repository root (quantum-ml-research/).
# Install everything with:  uv sync
dependencies = []
