[project]
name = "quantum-reg"
version = "0.1.0"
description = "Quantum Regularization — ablation study on VQC as implicit regularizer"
readme = "README.md"
requires-python = ">=3.12"
# All dependencies are managed from the repository root (quantum-ml-research/).
# Install everything with:  uv sync
dependencies = []
